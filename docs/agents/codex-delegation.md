# Codex Delegation Policy

Canonical source: `skills/skill-runner/references/codex-delegation.md`.

This repo-level page exists for human discoverability and project-level links.
Runtime skills should reference the `$skill-runner` bundled reference because
it is synced with the skill into `~/.codex/skills/skill-runner/references/`.
